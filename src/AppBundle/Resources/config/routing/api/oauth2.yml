
api_oauth2_decks:
    pattern: /decks
    methods: [GET]
    defaults:
        _controller: AppBundle:Oauth2:listDecks

api_oauth2_load_deck:
    pattern: /deck/load/{id}
    methods: [GET]
    defaults:
        _controller: AppBundle:Oauth2:loadDeck
    requirements:
        id: \d+

api_oauth2_save_deck:
    pattern: /deck/save/{id}
    methods: [PUT]
    defaults:
        _controller: AppBundle:Oauth2:saveDeck
    requirements:
        id: \d+

api_oauth2_publish_deck:
    pattern: /deck/publish/{id}
    methods: [PUT]
    defaults:
        _controller: AppBundle:Oauth2:publishDeck
    requirements:
        id: \d+
