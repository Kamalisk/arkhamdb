{% extends 'AppBundle:Admin:base.html.twig' %}

{% block content -%}
<h1>Faction</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ entity.code }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ entity.name }}</td>
            </tr>
            <tr>
                <th>isPrimary</th>
                <td>{{ entity.isPrimary }}</td>
            </tr>
            <tr>
                <th>OctgnId</th>
                <td>{{ entity.octgnId }}</td>
            </tr>
            </tbody>
    </table>

        <a href="{{ path('admin_faction') }}" class="btn btn-default">
            Back to the list
        </a>
        <a href="{{ path('admin_faction_edit', { 'id': entity.id }) }}" class="btn btn-default">
            Edit
        </a>
        <form action="{{ path('admin_faction_delete', { 'id': entity.id }) }}" method="post" style="display:inline-block">
            <input type="hidden" name="_method" value="DELETE" />
            {{ form_widget(delete_form) }}
            <button type="submit" class="btn btn-danger">Delete</button>
        </form>
{% endblock %}
